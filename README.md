# Python-Comparing-JSON-files
Comparing two JSON files to identify structural and value differences using Python. Implementing recursion, dictionary and list traversal, path tracking, and difference detection to highlight added, removed, and modified elements in nested JSON data structures.
